// This component is deprecated - authentication is now handled via /login page
export const AuthModal = ({ isOpen, onClose }: { isOpen: boolean; onClose: () => void; defaultTab?: string }) => {
  return null;
};
